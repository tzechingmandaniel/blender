Phase 6 Prompt 3 — Refine MAT_metal_dark_blued (dark blued steel)
=================================================================

Rebuild MAT_metal_dark_blued as a procedural shader:

  TextureCoordinate (Generated) -> Mapping -> Noise (scale 25, detail 6,
    distortion 0.2) -> ColorRamp("scratch_noise") -> drives:
      - Base Color   : mix from (0.05, 0.05, 0.07) (base blued steel) to
                       (0.10, 0.10, 0.13) (lighter highlights), factor 0.3
      - Roughness    : 0.40 +/- 0.15
      - Bump strength: 0.15
  Geometry.Pointiness -> ColorRamp(Constant, stops 0.40, 0.60) -> EdgeWear
    mix layer: blend Base Color toward (0.45, 0.43, 0.40) (exposed steel)
  AmbientOcclusion (samples=8, inside=False, only_local=True) -> ColorRamp
    -> CavityAO multiplier on Base Color (dark in cavities)

  Principled BSDF: Metallic = 1.0, Roughness = above, Specular IOR Level
    default. Normal driven by a Bump node with the noise.

Material identity:
  - Dark blued worn steel
  - Not mirror-shiny
  - Subtle scratches
  - Edge wear on exposed edges only (driven by Pointiness)
  - Cavities darkened by AO

Apply to all AK_metal_parts_grp objects.

Hard scope: no functional bore, no rifling, no chamber, no internal opening.

After running, verify:
  [ ] MAT_metal_dark_blued now uses a procedural graph
  [ ] EdgeWear group is referenced
  [ ] Receiver, dust cover, barrel still use this material
